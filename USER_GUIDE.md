# remote-iterm2 用户手册

> 通过手机浏览器，在局域网内远程控制 Mac 上的 iTerm2 终端。

---

## 目录

1. [快速开始](#1-快速开始)
2. [界面总览](#2-界面总览)
3. [顶部状态栏](#3-顶部状态栏)
4. [标签页管理栏](#4-标签页管理栏)
5. [终端内容区](#5-终端内容区)
6. [分屏视图](#6-分屏视图)
7. [窗口地图](#7-窗口地图)
8. [广播模式](#8-广播模式)
9. [快捷操作栏](#9-快捷操作栏)
10. [命令输入区](#10-命令输入区)
11. [虚拟键盘](#11-虚拟键盘)
12. [颜色语义说明](#12-颜色语义说明)
13. [PWA 安装](#13-pwa-安装)
14. [配置说明](#14-配置说明)

---

## 1. 快速开始

### 安装

```bash
npm install -g remote-iterm2
```

### 启动 / 停止 / 重启

```bash
remote-iterm2          # 启动服务
remote-iterm2 stop     # 停止服务
remote-iterm2 restart  # 重启服务
```

启动后终端会打印访问地址，例如：

```
http://192.168.1.119:7291
```

用手机 Safari 打开该地址（需与 Mac 在同一 Wi-Fi 下）即可开始使用。

### 系统要求

| 项目 | 要求 |
|------|------|
| macOS | 需安装 iTerm2 |
| Node.js | >= 18 |
| 端口 | 7291（HTTP + WebSocket 共用） |
| 手机端 | 与 Mac 处于同一局域网 |

---

## 2. 界面总览

```
┌─────────────────────────────────────────────────┐
│  顶部状态栏（连接状态 / 延迟 / 功能开关）          │
├─────────────────────────────────────────────────┤
│  标签页管理栏（Tab 切换 / 新建 / 关闭）            │
├─────────────────────────────────────────────────┤
│                                                  │
│  终端内容区（语法着色输出 / 光标）                 │
│                                                  │
├─────────────────────────────────────────────────┤
│  快捷操作栏（Enter / ESC / ^C / 方向键等）        │
├─────────────────────────────────────────────────┤
│  命令输入区（命令行 / 虚拟键盘切换）               │
└─────────────────────────────────────────────────┘
```

横屏模式下布局会自动压缩高度，优化空间利用率。

---

## 3. 顶部状态栏

位于屏幕顶部，从左到右依次显示：

### 3.1 连接状态指示灯

| 颜色 | 含义 |
|------|------|
| 🟢 绿色（有光晕动画） | 已连接到 Mac |
| 🔴 红色 | 连接断开，正在重连 |

断开连接时，全屏会出现半透明遮罩并显示 **RECONNECTING**，自动尝试重连，无需手动刷新。

### 3.2 延迟显示（ms）

连接成功后，每 5 秒自动测量一次往返延迟，并以颜色区分网络质量：

| 颜色 | 延迟范围 | 含义 |
|------|----------|------|
| 绿色 | < 50ms | 网络良好 |
| 黄色 | 50–149ms | 网络一般 |
| 红色 | ≥ 150ms | 网络较差 |

### 3.3 ALERT — 长命令完成提醒

点击 🔔 图标切换开关。

- **开启**（黄色高亮）：当终端输出停止超过 4 秒时，手机会**震动提醒**（双次短振），适合等待耗时命令（如编译、下载）完成时使用。
- **关闭**（默认灰色）：无震动。

### 3.4 SCROLL — 滚动锁定

点击 🔒/🔓 图标切换。

- **锁定**（红色 LOCK 图标）：终端内容**不会自动滚动到底部**，可自由查看历史输出。
- **解锁**（默认）：每次有新输出，终端自动滚动到最新行。

### 3.5 CAST — 广播模式

点击 📡 图标切换广播模式（详见 [第 8 节](#8-广播模式)）。

- **开启**（紫色高亮）：发送的命令会**同时广播到多个 iTerm2 窗口**。
- **关闭**（默认）：命令只发送到当前焦点窗口/标签页。

### 3.6 SPLIT — 分屏视图

点击 ⊟ 图标切换分屏（详见 [第 6 节](#6-分屏视图)）。

- **开启**（蓝色高亮）：屏幕分为上下（竖屏）或左右（横屏）两个独立终端面板。
- **关闭**（默认）：单面板模式。
- 仅有一个 Session 时此按钮变灰，不可点击。

### 3.7 N win — 窗口切换按钮

显示当前 iTerm2 **窗口总数**（如 `2 win`、`3 win`）。

- 点击后弹出[窗口地图](#7-窗口地图)，可切换到其他窗口。
- 只有 1 个窗口时按钮变灰，不可点击。

---

## 4. 标签页管理栏

显示当前所选 iTerm2 窗口的所有标签页（Tab）。

### 4.1 切换标签页

点击标签名即可切换，当前活跃标签以**绿色下划线**高亮显示。

> 分屏模式下，顶部标签栏显示的是**当前焦点面板**（主面板或分屏面板）对应的窗口标签页。

### 4.2 重命名标签页

**长按**标签名（约 0.5 秒），弹出输入框，输入新名称后回车确认。

### 4.3 新建标签页

点击标签栏最右侧的 **＋** 按钮，在 iTerm2 中新建一个标签页。

### 4.4 关闭标签页

点击标签栏最右侧的 **✕** 按钮，关闭当前活跃标签页。

> 仅剩一个标签页时，关闭按钮自动置灰，防止误操作。

---

## 5. 终端内容区

显示当前 Session 的终端输出，支持语法着色、滚动查看、光标显示。

### 5.1 语法着色规则

终端输出会自动按内容类型着色，方便快速识别关键信息：

| 颜色 | 代表内容 |
|------|----------|
| 亮白色（粗体） | 当前命令提示行（最后一行）|
| 青色（粗体） | Shell 提示符行（`user@host $`、`%`、`#` 等） |
| 红色 | 错误信息（error、FAIL、fatal、ENOENT 等） |
| 黄色 | 警告信息（warning、WARN、deprecated 等） |
| 绿色 | 成功信息（success、done、compiled、OK 等） |
| 紫蓝色 | 文件路径（`/`、`~/`、`./` 开头）及目录树符号 |
| 浅绿色 | Git diff 新增行（`+` 开头） |
| 粉红色 | Git diff 删除行（`-` 开头） |
| 暗灰色 | 注释行（`#` 开头） |
| 浅灰色 | 普通输出 |

### 5.2 光标

- 终端最后显示绿色闪烁方块光标 `█`（仅在 Shell 提示符状态下出现）。
- 运行交互式应用（如 `vim`、`htop`）时，光标自动隐藏，避免干扰。

### 5.3 滚动查看

直接在终端区域上下滑动即可查看历史输出。

若开启了[滚动锁定](#34-scroll--滚动锁定)，可自由浏览而不被新输出打断。

---

## 6. 分屏视图

同时查看两个不同 Session 的终端输出，适合对比日志、同时监控多个进程。

### 6.1 开启分屏

点击顶部状态栏的 **SPLIT** 按钮（⊟ 图标）：

- 需要至少有 **2 个 Session**（多窗口或多标签）才能启用。
- 开启后屏幕分为两个面板，**主面板**（绿色边框）显示当前 Session，**分屏面板**（蓝色边框）显示第二个 Session。

### 6.2 竖屏 / 横屏布局

| 方向 | 分屏方式 |
|------|----------|
| 竖屏 | 上下分屏 |
| 横屏 | 左右分屏 |

### 6.3 调整分屏比例

拖动两个面板之间的**分隔条**，可自由调整主面板与分屏面板的占比（限制在 20%–80% 之间）。

### 6.4 切换焦点面板

**点击**任意面板，该面板即变为焦点面板（边框变亮）。之后输入区域的命令、快捷键均发送到焦点面板对应的 Session。

### 6.5 分屏面板的标签栏

分屏面板有独立的**标签栏**，支持切换标签、新建/关闭标签。  
若存在多个 iTerm2 窗口，还可点击 **N win** 按钮弹出窗口地图，为分屏面板选择不同窗口。

### 6.6 关闭分屏

再次点击 **SPLIT** 按钮即可关闭分屏，恢复单面板模式。

---

## 7. 窗口地图

当 iTerm2 中存在多个窗口时，可通过窗口地图可视化切换。

### 7.1 打开方式

点击顶部状态栏右侧的 **N win** 按钮（如 `2 win`）。

### 7.2 界面说明

弹出半透明全屏遮罩，按比例缩放还原各 iTerm2 窗口在屏幕上的**实际位置和大小**。

- 当前选中窗口：绿色高亮边框
- 其他窗口：灰色边框，显示窗口编号

### 7.3 切换窗口

点击对应的窗口块即可切换到该窗口，地图自动关闭。  
点击遮罩空白区域也可关闭地图（不切换）。

### 7.4 广播模式下的窗口地图

开启广播模式后，窗口地图变为**多选模式**（标题变为 SELECT BROADCAST TARGETS），点击窗口块可切换其是否为广播目标（紫色高亮表示已选中）。

---

## 8. 广播模式

将同一条命令**同时发送到多个 iTerm2 窗口**。

### 8.1 开启广播

点击顶部状态栏的 **CAST** 按钮（📡 图标）：

- 默认选中**所有窗口**为广播目标。
- 标签页下方出现紫色横幅，显示当前广播目标数量（如 `BROADCAST TO 2 WINDOWS`）。

### 8.2 选择广播目标

点击广播横幅右侧的 **EDIT** 按钮，打开[窗口地图](#7-窗口地图)的多选模式，勾选/取消勾选需要广播的窗口。

### 8.3 发送广播命令

在命令输入框中输入命令，提示符变为 `>`（广播模式标识），点击发送后命令将同时发到所有选中窗口的活跃 Session。

### 8.4 关闭广播

再次点击 **CAST** 按钮，广播模式关闭，目标选择清空，恢复单窗口发送。

---

## 9. 快捷操作栏

位于终端内容区下方，提供高频终端操作的一键按钮，无需打开键盘即可操作。

### 9.1 控制键（红/黄色）

| 按钮 | 功能 | 发送字符 |
|------|------|----------|
| ↵（Enter） | 发送回车 | `\n` |
| ESC | 发送 Escape 键 | `\x1b` |
| ^C | 中断当前进程（Ctrl+C） | `\x03` |
| ^D | 发送 EOF / 退出（Ctrl+D） | `\x04` |
| ^Z | 挂起进程到后台（Ctrl+Z） | `\x1a` |
| ^L | 清屏（Ctrl+L） | `\x0c` |

### 9.2 光标导航键（紫色）

| 按钮 | 功能 |
|------|------|
| ↑（上箭头） | 上方向键（用于命令历史、菜单导航） |
| ↓（下箭头） | 下方向键 |
| TAB | 发送 Tab 键（自动补全） |
| ⇧TAB | 发送 Shift+Tab（反向补全 / 反向切换选项） |

### 9.3 剪贴板操作（蓝/灰色）

| 按钮 | 功能 |
|------|------|
| 📋（粘贴） | 从手机剪贴板读取内容，直接发送到终端 |
| 复制 | 将**当前终端内容**复制到手机剪贴板 |
| 🗑️（清除行） | 清除当前命令行输入（Ctrl+U，`\x15`） |

> 粘贴时会请求剪贴板权限，iOS 需在弹窗中确认允许。

---

## 10. 命令输入区

位于屏幕底部，是输入和发送命令的主要区域。

### 10.1 命令输入框

- 输入框左侧的 `$` 或 `>` 是模式提示符：
  - `$`：普通模式，发送到当前焦点 Session
  - `>`：广播模式，发送到所有选中窗口
- 支持 iOS 原生键盘输入，已禁用自动纠错和首字母大写。
- 输入框右侧为**发送按钮**（Send 图标），有内容时高亮；无内容或未连接时变灰禁用。

### 10.2 发送命令

- 点击发送按钮，或在键盘上点击 **发送 / Return** 键（`enterKeyHint="send"` 模式）。
- 发送后命令自动保存到历史记录（最多保留 100 条，存储于 localStorage）。

### 10.3 命令历史导航

在命令输入框中，使用输入设备（支持外接蓝牙键盘）：

| 操作 | 说明 |
|------|------|
| ↑ 方向键 | 向历史记录前翻（更早的命令） |
| ↓ 方向键 | 向历史记录后翻（更近的命令） |

历史浏览时，原始输入内容会被暂存；退出历史浏览后自动恢复。

> 配合快捷操作栏的 ↑↓ 按钮，可在交互式程序（如 `fzf`、`python` REPL）中导航。

### 10.4 切换虚拟键盘

点击输入区左侧的 ⌨️ 按钮，切换到[虚拟键盘模式](#11-虚拟键盘)，适合需要输入特殊符号时使用。

---

## 11. 虚拟键盘

专为终端操作设计的内置软键盘，提供字母、数字及常用符号布局，无需依赖系统键盘。

### 11.1 进入 / 退出

- **进入**：点击命令输入区左侧的 ⌨️ 按钮。
- **退出**：点击虚拟键盘底部左侧的 🖥️（Terminal）按钮，返回命令输入框。

### 11.2 字母模式（ABC）

- 三行标准 QWERTY 布局（`q w e r t y ...`）
- 左下角 **SHIFT** 键：点击后下一个字母以大写发送，自动单次大写（类似 Shift 锁）
- 右下角 **DEL** 键：发送退格键（`\x7f`）

### 11.3 符号模式（123 / #+= ）

按底部 **123** 键切换：

| 模式 | 内容 |
|------|------|
| `123`（sym） | 数字 0–9，`-` `/` `.` `_` `~` `*` `$` `\|` `>` `<` `&` `;` `=` `"` `'` `\` `` ` `` `#` |
| `#+= `（sym2） | `!` `@` `%` `^` `+` `=` `?` `:` `(` `)` `[` `]` `{` `}` `,` `;` `"` `'` `` ` `` `~` `\` `_` |

在符号模式下，左下角按钮在两个符号组之间切换（`#+= ` ↔ `123`）。

### 11.4 底部功能行

| 按钮 | 功能 |
|------|------|
| 🖥️ | 退出虚拟键盘，返回命令输入框 |
| ABC / 123 | 切换字母 / 符号模式 |
| `-` | 快速输入连字符 |
| SPACE | 发送空格 |
| `.` | 快速输入句点 |
| ↵ | 发送回车 |

---

## 12. 颜色语义说明

整个界面使用统一的颜色系统，便于快速识别状态：

| 颜色 | 用途 |
|------|------|
| 🟢 绿色（`#10b981`） | 主色调：连接正常、当前选中 Tab、主面板边框、发送按钮 |
| 🔵 青蓝色（`#38bdf8`） | 分屏面板标识色 |
| 🟣 紫色（`#818cf8`） | 广播模式标识色 |
| 🟡 黄色（`#fbbf24`） | 网络延迟中等、ALERT 开启状态 |
| 🔴 红色（`#f87171`） | 连接断开、网络延迟差、滚动锁定开启 |

---

## 13. PWA 安装

将 remote-iterm2 添加为手机主屏幕应用，获得全屏无浏览器栏的体验：

1. 在 iPhone Safari 中打开应用 URL（如 `http://192.168.1.119:7291`）
2. 点击底部分享按钮 → **添加到主屏幕**
3. 确认名称后点击**添加**

安装后图标出现在主屏幕，点击即可全屏启动，自动处理 Dynamic Island / 刘海屏安全区域。

---

## 14. 配置说明

在 Mac 上创建 `~/.remote-iterm2.json` 文件可自定义行为：

```json
{
  "tabColor": false
}
```

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `tabColor` | `true` | 是否在手机切换窗口时自动改变 iTerm2 标签背景色。设为 `false` 可保留手动设置的标签颜色。 |

---

## 常见问题

**Q: 手机打开页面一直显示 RECONNECTING？**  
A: 请确认手机和 Mac 在同一 Wi-Fi 下，且 Mac 已运行 `remote-iterm2`（端口 7291 未被防火墙拦截）。

**Q: 发送 Enter 后命令没有执行？**  
A: 请使用快捷操作栏的 **↵（Enter）** 按钮，或确保输入框内容发送后通过 Send 按钮提交。

**Q: 粘贴按钮没有效果？**  
A: iOS 首次使用需授权剪贴板访问权限，在弹窗中选择「允许粘贴」。

**Q: 终端输出乱码 / ANSI 颜色代码未处理？**  
A: 当前版本以纯文本着色（正则匹配语义）而非解析 ANSI 转义序列，部分带 ANSI 颜色的程序输出可能显示原始转义字符，属于已知限制。

---

*本手册基于 remote-iterm2 v1.2.6 编写。*
